
<%= form_for :article, url: user_article_path(@user.id,@article.id), method: :patch do |f| %>
	<%= f.label :article_name %><br>
	<%= f.text_area :title, :rows => 5, :cols=>20 %>
	<%= f.submit 'Submit' %>
<% end %>